#include <bits/stdc++.h>
typedef long long LL;
#define REP(i, a) REPP(i, 0, (a) - 1)
#define REPP(i, a, b) for (int i = int(a); i <= int(b); i++)
using namespace std;

int main() {
  int t, ca = 1;
  cout << 10 << endl;
  REPP(i, 1, 10) {
    cout << 3 << ' ' << 3 << ' ' << 3 << ' ' << i << endl;
  }
  return 0;
}



